# Demanem a l'usuari que posi un numero qualsevol
numero = int(input("Introdueix el numero que vulguis: "))

# Calcular la suma
suma_total = sum(range(1, numero + 1))

# Mostrar el resultat de la suma
print(f"Numero indicat per l'usuari: {numero}")
print(f"La suma es: {suma_total}")